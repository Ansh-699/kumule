[package]
name = "event-escrow"
version = "0.1.0"
description = "Event escrow program for Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "event_escrow"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.0"
anchor-spl = "0.30.0"

